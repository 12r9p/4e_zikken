#import "../lib/report-utils.typ": *
#show: setup_report

// ========== ここを編集 ==========
#let title = "デジタル信号処理"
#let place = "電子計測実験室"
#let date = "2026年6月26日"
#let whether = ""
#let teacher = "服部 教員"
// ================================

#v(10em)
#align(center, text(size: 16pt, weight: "bold", [#title]))
#v(6em)
#align(right, [
  2026年 4E 電気電子システム工学実験
])
#v(40em)
#align(left, [
  作成者： 佐藤 匠
])
#align(left, [
  作成日： #datetime.today().display("[year]年[month]月[day]日")
])
#align(left, [
  実験日： #date
])
#align(left, [
  天候： #whether
])
#align(left, [
  実験場所： #place
])
#align(left, [
  担当教員： #teacher
])

#pagebreak()

// ================================

= 目的
マイクロコンピュータを用いた音声信号処理の実験を通して、デジタル信号処理の基礎知識を修得する。

= 原理

アナログ信号をデジタル信号へ変換する処理は、標本化、量子化、符号化の三段階で行われる。標本化とは、連続時間信号を一定時間間隔 $Delta t$ で取り出すことであり、その逆数 $f_s = 1 / Delta t$ をサンプリング周波数という。量子化では標本化した信号の振幅を離散的な数値に変換し、符号化ではその数値を2進数で表現する。

周期信号は、基本周波数とその整数倍の周波数をもつ正弦波・余弦波の和として表せる。この表現をフーリエ級数展開といい、周期 $T$、基本角周波数 $omega_0 = 2 pi / T$ の信号 $g(t)$ は、

$ g(t) = a_0 + sum_(n=1)^infinity (a_n cos(n omega_0 t) + b_n sin(n omega_0 t)) $

と表される。各係数は

$ a_0 = 1/T integral_0^T g(t) dif t $

$ a_n = 2/T integral_0^T g(t) cos(n omega_0 t) dif t $

$ b_n = 2/T integral_0^T g(t) sin(n omega_0 t) dif t $

で与えられる。また、複素フーリエ級数では

$ g(t) = sum_(n=-infinity)^infinity c_n e^(j n omega_0 t), quad c_n = 1/T integral_0^T g(t)e^(-j n omega_0 t) dif t $

と表せる。

フーリエ変換は時間領域の信号を周波数領域へ変換し、信号に含まれる周波数成分を解析する方法である。角周波数を用いる場合は

$ G(omega) = integral_(-infinity)^infinity g(t)e^(-j omega t) dif t $

であり、周波数 $f$ を用いる場合は

$ G(f) = integral_(-infinity)^infinity g(t)e^(-j 2 pi f t) dif t $

となる。逆フーリエ変換では正の指数を用いて時間信号を復元する。

サンプリング定理より、信号に含まれる最高周波数の2倍以上のサンプリング周波数で標本化すれば、元の信号を再現できる。サンプリングされた信号のフーリエ変換は周波数軸上で $f_s$ 周期となるため、ナイキスト周波数 $f_s/2$ を超える成分は折り返され、エイリアシングが生じる。本実験ではサンプリング周波数を $f_s = 48\,"kHz"$ とした。FFT のサンプル数を $N$ とすると、周波数分解能は

$ Delta f = f_s / N $

である。

= 結果

以下では、CSVに保存されたFFTデータを周波数軸へ変換して振幅スペクトルを作成した。各データの周波数軸は $f_k = k f_s/N$ とし、グラフの縦軸はデータの振幅値、横軸は周波数とした。

== 3.1 単色音声信号のフーリエ変換

ファンクションジェネレータ等から入力した500 Hz、1 kHzの正弦波および矩形波をFFTし、振幅スペクトルを比較した。使用したデータは、正弦波がdata016（500 Hz）とdata033（1 kHz）、矩形波がdata028（500 Hz）とdata033（1 kHz）である。実験結果を図1〜図4に示す。

#figure(image("figures/square_1000Hz.png", width: 100%), caption: [1 kHz 矩形波の振幅スペクトル])
#figure(image("figures/sine_1000Hz.png", width: 100%), caption: [1 kHz 正弦波の振幅スペクトル])
#figure(image("figures/square_500Hz.png", width: 100%), caption: [500 Hz 矩形波の振幅スペクトル])
#figure(image("figures/sine_500Hz.png", width: 100%), caption: [500 Hz 正弦波の振幅スペクトル])

正弦波は理論上、設定した基本周波数の成分だけをもつ。そのため、図2および図4では、それぞれ1 kHz付近および500 Hz付近に主ピークが現れ、他の成分は小さかった。一方、理想的な矩形波は基本波と奇数次高調波から構成され、高調波の振幅は次数が増えるほど小さくなる。図1および図3でも、基本周波数に加えて約3 kHz、5 kHzなどの奇数倍周波数にピークが現れた。ただし、実測値ではマイク、アンプ、A/D変換、窓関数、有限長データの影響により、理論値からのずれや不要な成分が生じたと考えられる。

/* 検討1: ここに各グラフで読み取った主ピーク周波数と理論値との差を追記する。 */

== 3.2 サンプル数と振幅スペクトルの関係

500 Hz矩形波について、FFTのサンプル数を1024、256、64として比較した。$f_s = 48\,"kHz"$ のとき、周波数分解能はそれぞれ46.875 Hz、187.5 Hz、750 Hzである。サンプル数ごとの結果を図5〜図7に示す。

#figure(image("figures/sample_count_1024.png", width: 100%), caption: [サンプル数1024点の振幅スペクトル])
#figure(image("figures/sample_count_256.png", width: 100%), caption: [サンプル数256点の振幅スペクトル])
#figure(image("figures/sample_count_64.png", width: 100%), caption: [サンプル数64点の振幅スペクトル])

図5〜図7より、サンプル数を減らすほど周波数ビンの間隔が広がり、ピークの位置が粗くなった。64点では500 Hz付近の成分や高調波を細かく分離しにくく、256点、1024点の順にピークの位置と形状を確認しやすくなった。これは $Delta f = f_s/N$ より、サンプル数の増加に伴って周波数分解能が向上するためである。したがって、FFTで周波数成分を精度よく調べるには、目的とする周波数分解能に応じて十分なサンプル数を確保する必要がある。

/* 検討2: 図5〜図7から、1024点・256点・64点それぞれのピークの見え方を自分の観察に合わせて補足する。 */

== 3.3 フーリエ変換・逆フーリエ変換後の音質

VoiceChangerのMaincoreおよびSubcore1を用い、マイクから入力した音声をFFTし、逆FFTによって時間波形へ戻してスピーカーから出力した。サンプル数を半分にして処理した場合、レジュメの記録では出力音声が「ボコボコ」した音になった。

サンプル数を半分にすると、同じサンプリング周波数でも周波数分解能が低下する。また、短い処理区間ごとに音声を復元するため、音声の急激な変化や高周波成分を十分に表現できず、フレーム間のつながりも不自然になる。この結果、逆FFT後の波形が入力波形を十分に再現できず、音質の粗さや歪みとして聞こえたと考えられる。

/* 検討3: 元の音声とサンプル数を半分にした音声を聴き比べ、音量・音高・高音域・歪み・雑音について具体的に追記する。 */

== 3.4 入出力音声の振幅スペクトルと設定値

フーリエ変換による周波数解析1では、メモに従い、スマートフォンから約1200 Hzの正弦波を入力し、スピーカーから出力された音を再びスマートフォンで取得した。pitch_shiftを-10（data009）および-20（data003）に設定したデータを用いた。結果を図8、図9に示す。

#figure(image("figures/pitch_shift_minus10.png", width: 100%), caption: [pitch_shiftを-10に設定したときの振幅スペクトル])
#figure(image("figures/pitch_shift_minus20.png", width: 100%), caption: [pitch_shiftを-20に設定したときの振幅スペクトル])

図8、図9より、pitch_shiftの設定値を変えると、振幅スペクトルのピーク位置と周辺成分の分布が変化した。pitch_shiftは周波数をHz単位で直接指定する値ではなく、FFTで得た振幅スペクトルを移動させる量である。そのため、設定値の変更によって基本周波数成分の現れる位置が変わり、出力音声の音高にも変化が生じたと考えられる。また、スペクトルの移動に伴って高周波成分や不要成分の分布も変化するため、音高だけでなく音色や歪みの聞こえ方も変化すると考えられる。

/* 検討4: -10と-20の音声を実際に聴き比べ、音高、音質、音量、歪みの違いを追記する。 */

== 3.5 母音の振幅スペクトル

フーリエ変換による周波数解析2で取得したデータから、メモに記録された時刻（「あ」14秒、「い」20秒、「う」24秒、「え」28秒、「お」33秒）に対応するデータを取り出した。各母音の振幅スペクトルを図7〜図11に示す。

#figure(image("figures/vowel_あ.png", width: 100%), caption: [母音「あ」の振幅スペクトル])
#figure(image("figures/vowel_い.png", width: 100%), caption: [母音「い」の振幅スペクトル])
#figure(image("figures/vowel_う.png", width: 100%), caption: [母音「う」の振幅スペクトル])
#figure(image("figures/vowel_え.png", width: 100%), caption: [母音「え」の振幅スペクトル])
#figure(image("figures/vowel_お.png", width: 100%), caption: [母音「お」の振幅スペクトル])

図7〜図11を比較すると、母音ごとにピークの位置や高周波側の成分分布が異なった。これは、母音によって声道の形状が変化し、共鳴周波数であるフォルマントが変わるためである。したがって、母音は時間波形だけでなく、周波数領域のスペクトルの違いからも特徴付けられる。

/* 検討5: 現在のデータは一人分である。班員の同じ母音のスペクトルを追加し、共通するピークと個人差を比較して追記する。 */

= 検討

本実験では、正弦波、矩形波、音声および母音のFFT結果を比較した。正弦波では設定周波数付近に主成分が集中し、矩形波では基本波と奇数次高調波が現れたことから、実験結果はそれぞれの理論的なフーリエ変換の特徴と対応した。また、FFTのサンプル数を増やすと $Delta f = f_s/N$ に従って周波数分解能が向上し、高調波を分離しやすくなった。

さらに、サンプル数を半分にしたFFT・逆FFTでは出力音声が「ボコボコ」した音になり、周波数分解能と音質の関係を確認できた。pitch_shiftを変更した実験では、振幅スペクトルのピーク位置と音声の音高・音色が変化した。母音では、母音ごとにスペクトルのピークや高周波成分が異なり、声道の共鳴が音声の特徴に関係することが分かった。

以上より、マイクロコンピュータを用いた音声信号処理を通して、時間領域の音声を周波数領域で解析する方法、FFTのサンプル数が解析精度に与える影響、および周波数成分を操作して音声を変化させる方法を理解できた。

/* TODO: 表紙の天候、共同実験者、使用機器の型番、および検討1〜5の実聴・班員比較の具体値を確認して完成させる。 */
